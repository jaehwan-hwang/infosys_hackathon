# 정보시스템학과 해커톤 — 기획 및 웹사이트 개발 계획서

> 이 문서는 지금까지 논의한 해커톤 운영 계획과, 이를 실제로 구현할 웹사이트의 백엔드·프론트엔드 설계를 한 파일에 정리한 초안입니다. `[ ]`로 표시된 항목은 아직 확정되지 않아 채워 넣어야 하는 값입니다.

---

## 1. 개요

| 항목 | 내용 |
|---|---|
| 주최 | 정보시스템학과 학생회 |
| 행사명 | `[해커톤 이름 미정]` |
| 주제(테마) | `[있다면 기입]` |
| 진행 기간 | 2일 (1일차 / 2일차) |
| 참가 자격 | `[정보시스템학과 한정 여부, 팀 인원 제한 등 미정]` |
| 참가 신청 기간 | `[미정]` |
| 시상 내역 | `[트랙별 상금·상장 규모 미정]` |
| 문의처 | `[오픈채팅 링크 또는 이메일 미정]` |
| 필수 수집 정보 | 팀명, 팀원 성명, 학번, 한양대학교 이메일, 조장 여부 |

웹사이트는 크게 세 가지 역할을 한다: ① 해커톤을 소개하는 랜딩 페이지, ② 팀 등록·트랙별 산출물 제출, ③ 학생 투표 및 교수 평가를 거쳐 트랙별 수상팀을 가리는 채점 시스템.

---

## 2. 트랙 구성

세 트랙은 산출물의 완성도 단계에 따라 나뉘며, 트랙마다 제출물과 평가 기준이 다르다.

| 트랙 | 목표 | 제출물 | 평가 중점 |
|---|---|---|---|
| **Spark** (아이디어톤) | 제시된 문제를 해결하는 아이디어 제시 | 서비스 기획서, 프로토타입 (구현된 코드·구동 프로그램 제출 **전면 금지**, 순수 기획·UX 논리로만 평가) | 문제 정의 및 타당성, UI/UX 완성도, 서비스 실현 가능성 |
| **Sprint** | 문제를 해결하는 기초적인 프로그램 개발 | 템플릿 기반 소스코드, 제출물 발표자료, 핵심 기능 시연 | 핵심 기능 동작성, 기획 적합성 및 UX, 발표·코드 이해도 |
| **Summit** | 문제를 해결하는 완성된 프로그램 개발 | 실제 배포된 서비스, 시스템 아키텍처 다이어그램 및 기술 명세서, 전체 소스코드, 발표자료 | 기술적 난이도 및 완성도, 시스템 아키텍처 및 안정성, 서비스 완성도 및 상용화 가능성, 발표 |

### 2-1. 트랙 배정 기준

**즉시 Summit 트랙으로 배정되는 사유** (아래 중 하나라도 해당 시)
- 개발 직무 실무·인턴 경험이 있는 경우
- 교내외 개발 대회 수상 이력이 있는 경우
- 실제 배포·운영 서비스를 보유한 경우

**자가 진단 체크리스트** (아래 4개 중 **3개 이상** 해당 시 Summit 트랙으로 자동 지정)
1. API 및 서버 연동 경험 — 백엔드 프레임워크와 DB를 직접 구축·연동해 본 경험이 있다
2. 협업 툴 및 버전 관리 — Git/GitHub를 활용해 브랜치 전략을 통한 팀 프로젝트 경험이 있다
3. 심화 프로젝트 과목 이수 — 개발 산출물이 필수인 심화 전공 과목을 이수했다
4. 외부 API/라이브러리 활용 — 외부 인증, 결제 모듈, LLM/AI API 등을 연동한 프로젝트 경험이 있다

팀은 등록 시 Spark(1일차 전용) 또는 Sprint/Summit(2일차) 중 하나를 선택하며, Sprint/Summit을 선택한 팀은 위 체크리스트를 거쳐 Sprint 또는 Summit으로 최종 배정된다.

---

## 3. 진행 일정

### 1일차 — Spark 트랙 전용

| 시간 | 내용 |
|---|---|
| 10:00 | 시작 |
| 13:00 | 점심 식사 |
| ~19:00 | Spark 트랙 산출물 제출 마감 |
| 19:00 | 저녁 식사 + Spark 트랙 산출물 평가 (발표 종료 직후 학생 투표) |
| 21:00 | Spark 트랙 시상 |
| 22:00 | 해산 |

### 2일차 — Sprint / Summit 트랙

| 시간 | 내용 |
|---|---|
| 10:00 | 시작 |
| 13:00 | 점심 식사 |
| ~18:00 | Sprint, Summit 트랙 산출물 제출 마감 (제출 직후 심사 시작) |
| 18:00–19:00 | 저녁 식사 |
| 19:00–20:00 | Sprint 프레젠테이션 |
| 20:00–21:00 | Summit 프레젠테이션 |
| 21:00–22:00 | Sprint, Summit 평가 |
| 21:00 | Sprint, Summit 트랙 시상 |
| 22:00 | 해산 |

Spark 트랙은 1일차만 진행되는 별도 트랙이며, Sprint/Summit 트랙 참가팀은 2일차 일정으로 진행된다.

---

## 4. 참가 플로우

```
팀 등록 (팀명·팀원·조장·한양대 이메일·학번, 트랙 선택 + 자가진단)
   │
   ├─ Spark 선택 → 1일차 19:00 제출 마감 → 발표 → 학생 투표(100%) → 시상 → 종료
   │
   └─ Sprint/Summit 선택(자가진단 결과 반영) → 2일차 18:00 제출 마감 → 발표
         ├─ Sprint → 학생 투표(100%)
         └─ Summit → 학생 투표(30%) + 교수 평가(70%) 가중 합산
       → 시상
```

- 순위·점수는 **시상식 발표 전까지 비공개**로 유지한다 (참가팀에게 실시간 노출하지 않음).
- 제출 마감 시각이 지나면 해당 트랙의 제출 폼은 자동으로 잠긴다.

---

## 5. 트랙별 제출 요구사항

| 구분 | Spark | Sprint | Summit |
|---|---|---|---|
| 서비스 기획서 | ✅ (필수) | 선택 | 선택 |
| 프로토타입(목업/와이어프레임) | ✅ (필수) | — | — |
| 소스코드 | ❌ (제출 금지) | ✅ (템플릿 기반) | ✅ (전체 소스코드) |
| 배포 링크 | — | — | ✅ |
| 시스템 아키텍처 다이어그램 | — | — | ✅ |
| 기술 명세서 | — | — | ✅ |
| 발표자료 (PPT/PDF) | 선택 | ✅ | ✅ |
| 핵심 기능 시연 (영상 또는 링크) | — | ✅ | 선택 |

---

## 6. 평가 체계

| 트랙 | 평가 주체 및 가중치 | 비고 |
|---|---|---|
| Spark | 참가 학생 투표 100% | 발표 종료 직후 각자 개인 화면에서 팀별 평가 |
| Sprint | 참가 학생 투표 100% | 동일 |
| Summit | 교수 평가 70% + 학생 투표 30% | 교수 평가는 별도 접근 페이지에서 진행 |

**중복/자기 투표 방지**: 평가 시 투표자의 팀명(또는 학번)을 함께 기록해 팀당 1회만 반영되도록 하고, 발표팀 본인의 팀은 투표 대상에서 제외한다.

**최종 점수 산식 (Summit)**
```
최종 점수 = 교수 평가 평균 × 0.7 + 학생 투표 평균 × 0.3
```

---

## 7. 웹사이트 기능 명세

1. **메인 페이지** — 학과명 + 해커톤명 히어로, 스크롤 시 개요 → 트랙 소개 → 자가진단 → 당일 타임라인 → 발표자료(PPT) 순으로 노출
2. **트랙 자가진단** — 체크리스트를 인터랙티브 폼으로 제공, 결과(권장 트랙)를 즉시 안내
3. **팀 등록** — 팀명, 조장/팀원 정보(성명·학번·한양대 이메일), 트랙 선택 및 자동 배정 로직 반영
4. **산출물 제출** — 팀의 트랙에 따라 제출 항목이 동적으로 바뀌는 폼, 마감 시각 이후 자동 잠금, 파일 업로드 지원
5. **학생 평가** — 발표 종료 후 각자 기기에서 접속해 팀을 채점하는 설문형 페이지 (Spark/Sprint/Summit 공통, 트랙 내에서만 채점)
6. **교수 평가** — Summit 트랙 전용, 별도 접근 경로(비밀번호 또는 학교 이메일 인증)로 채점
7. **관리자 대시보드** — 학생회 전용. 전체 팀 목록·제출 파일·채점 현황 확인, 트랙별 순위 자동 집계, 데이터 내보내기(CSV/Excel)

---

## 8. 기술 아키텍처

### 8.1 왜 별도의 백엔드가 필요한가

Claude 아티팩트에는 자체 데이터베이스 기능(`db` capability)이 있지만, 이는 **아티팩트 제작자와 같은 Claude 조직(Organization)에 로그인한 사용자만 읽고 쓸 수 있는 구조**다. 학과 학생·교수 전체가 접속해 팀 등록·제출·투표를 해야 하는 이 서비스에는 맞지 않고, 파일 업로드 기능도 현재 계정에서 제공되지 않는다. 따라서 실제 데이터를 다루는 부분은 별도의 백엔드(DB + 파일 저장소 + 인증)를 갖춘 독립 웹앱으로 구현하는 것을 권장한다.

### 8.2 전체 구조

```
[Next.js 프론트엔드 (Vercel 배포)]
        │  Firebase SDK
        ▼
[Firebase Authentication] — Google 로그인, @hanyang.ac.kr 도메인 제한
        │
        ▼
[Cloud Firestore]              [Firebase Storage]
  teams / submissions /          기획서, 프로토타입, 소스코드(zip),
  scores / adminRoles            발표자료, 아키텍처 다이어그램 등 파일
```

### 8.3 백엔드 / DB 설계

**추천 스택**: Firebase (Firestore + Authentication + Storage + Hosting)
소규모·단기 이벤트에 적합한 무료 티어, 한국어 자료가 풍부해 학생회 내에서 유지보수하기 쉽다는 점이 이유. 관계형 쿼리가 많이 필요하다면 Supabase(PostgreSQL 기반)도 대안이 될 수 있다.

**인증 설계**
- Firebase Authentication의 Google 로그인 사용, 인가된 도메인을 `@hanyang.ac.kr`으로 제한 → 한양대 이메일 여부를 별도 검증 로직 없이 로그인 단계에서 확인
- 로그인된 사용자의 학번·성명은 최초 로그인 시 프로필 입력 폼에서 1회 수집해 `users` 컬렉션에 저장
- 교수 평가 페이지는 `adminRoles` 컬렉션에 등록된 이메일만 접근하도록 라우트 가드 적용
- 관리자(학생회) 페이지도 동일한 방식으로 역할(`role: 'admin' | 'professor' | 'student'`) 기반 접근 제어

**데이터 모델 (Firestore 컬렉션)**

```
users/{uid}
  name, studentId, email, role  // role: student | professor | admin

teams/{teamId}
  teamName, track,              // track: spark | sprint | summit
  leaderUid,
  members: [{ uid, name, studentId, email }],
  selfCheck: { apiExperience, gitCollab, advancedCourse, externalApi },
  autoAssignReason,              // 즉시 Summit 배정 사유 (해당 시)
  createdAt

submissions/{teamId}
  teamId, track, day,            // day: 1 | 2
  planFileUrl, prototypeFileUrl,        // Spark
  sourceCodeFileUrl, deckFileUrl, demoUrl,  // Sprint
  deployUrl, architectureFileUrl, techSpecFileUrl,  // Summit
  submittedAt, locked

scores/{scoreId}                 // scoreId = `${voterUid}_${targetTeamId}`
  voterUid, voterRole,           // student | professor
  targetTeamId, track,
  criteria: { c1, c2, c3, c4 },  // 트랙별 평가 기준 점수
  total,
  createdAt
```

**보안 규칙 개요 (Firestore Security Rules)**
- `teams`, `submissions`: 로그인 사용자 누구나 읽기 가능 / 쓰기는 본인 팀 문서에 한정, 마감 시각 이후 클라이언트 쓰기 차단(Cloud Function으로 이중 검증 권장)
- `scores`: 문서 ID에 투표자 uid를 포함시켜 팀당 1회만 쓰기 가능하도록 강제, 자신의 팀 문서에는 쓰기 차단
- `adminRoles`: 관리자만 쓰기 가능, 나머지는 읽기 전용

**파일 저장 (Firebase Storage)**
```
/submissions/{teamId}/plan.pdf
/submissions/{teamId}/prototype.*
/submissions/{teamId}/source.zip
/submissions/{teamId}/deck.pptx
/submissions/{teamId}/architecture.png
```
업로드 용량 제한(예: 50MB)을 프론트엔드에서 사전 안내.

### 8.4 프론트엔드 설계

**추천 스택**: Next.js (App Router) + TypeScript + Tailwind CSS, Vercel 배포

**페이지 구조**
```
/                       메인 랜딩 (개요, 트랙 소개, 타임라인, PPT)
/self-check             트랙 자가진단
/register               팀 등록 (로그인 필요)
/submit                 산출물 제출 (팀의 트랙에 따라 동적 렌더링)
/evaluate                학생 평가 (발표 종료 후 활성화)
/evaluate/professor      교수 평가 (Summit 전용, 역할 가드)
/admin                   관리자 대시보드 (역할 가드)
```

**핵심 구현 포인트**
- 자가진단 로직은 완전히 클라이언트 계산: 체크된 항목 수 ≥ 3 → Summit 자동 표시, 등록 폼에 결과 반영
- 제출 폼은 팀의 `track` 필드를 읽어 조건부 렌더링 (Spark/Sprint/Summit 필드 세트 분리)
- 마감 시각은 서버 시간 기준(Cloud Function 또는 Firestore 서버 타임스탬프)으로 판정해 클라이언트 시계 조작을 방지, 남은 시간 카운트다운 UI 제공
- 평가 페이지는 발표 세션(트랙·일차)이 열려 있을 때만 활성화되도록 관리자가 상태를 토글
- 관리자 대시보드는 Firestore 실시간 구독(`onSnapshot`)으로 제출 현황을 실시간 갱신, 트랙별 최종 점수(Summit은 가중 합산)를 자동 계산해 순위표 제공, CSV 내보내기 버튼 포함
- 반응형 레이아웃 필수 (참가자 다수가 모바일로 투표·제출 접근)

---

## 9. 아직 정해야 할 항목 (TODO)

- [ ] 해커톤 정식 명칭 및 테마
- [ ] 참가 신청 기간, 정확한 행사 날짜
- [ ] 참가 자격 세부 조건 (타 학과 허용 여부, 팀 인원 상한/하한)
- [ ] 트랙별 시상 내역(상금·상장 규모)
- [ ] 관리자/교수 평가 페이지 접근 방식 확정 (Firebase 역할 기반 vs 별도 비밀번호)
- [ ] 문의처 (오픈채팅 링크 또는 이메일)
- [ ] 개인정보 수집·이용 동의 문구 (수집 목적, 보유·파기 기간)
- [ ] 파일 업로드 용량 제한 값

---

## 10. 참고 — Claude 아티팩트 활용 범위

위 백엔드가 준비되기 전, 정보 전달용 랜딩 페이지(트랙 소개, 자가진단, 타임라인, 규정 안내)는 Claude 아티팩트로 먼저 만들어 배포하고, 실제 등록·제출·평가 기능이 연결되는 대로 버튼/링크를 붙이는 단계적 진행도 가능하다.
